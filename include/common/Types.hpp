#pragma once

#include <cstdint>
#include <filesystem>
#include <functional>
#include <optional>
#include <string>
#include <vector>

namespace pou {

using FsPath = std::filesystem::path;
using std::function;
using std::optional;
using std::string;
using std::uint64_t;
using std::uintmax_t;
using std::vector;

} 
